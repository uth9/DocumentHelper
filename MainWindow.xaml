<hc:Window x:Class="DocumentHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentHelper"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:properties="clr-namespace:DocumentHelper.Properties"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="文档辅助器" Height="600" Width="1056" Icon="/Icon.png"
        NonClientAreaBackground="#408CCCCC">
    <Window.Resources>
        <properties:Settings x:Key="AppSettings"/>
        <Style x:Key="DataGridComboBoxCenterColumnStyle" TargetType="DataGridCell" BasedOn="{StaticResource DataGridCellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="True"/>
                        <Condition Property="Selector.IsSelectionActive" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="Transparent"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DataGridDefaultCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource DataGridCellStyle}">
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="False">
                    <Setter Property="Background" Value="#00000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </Window.Resources>

    <Grid>
        <!-- 背景图片 -->
        <Image Source="/background.png" Stretch="UniformToFill" Opacity="1" Margin="0"/>

        <!-- 磨砂玻璃层 -->
        <Border Background="#B3FFFFFF" CornerRadius="8" Margin="0">
            <Border.Effect>
                <BlurEffect Radius="15" KernelType="Gaussian"/>
            </Border.Effect>
        </Border>

        <hc:SimpleStackPanel Margin="5">
            <!-- 标题栏 - 使用Border添加白色半透明背景 -->
            <Border Background="#80FFFFFF" CornerRadius="4" Margin="5">
                <hc:SimpleStackPanel VerticalAlignment="Top" Height="60" 
                                     Orientation="Horizontal" 
                                     Background="Transparent">
                    <!--新建条目，打开【AddNewRowWindow】-->
                    <Button x:Name="AddNewRowButton" Height="45" FontSize="14" Margin="5,0,5,0" Click="AddNewRowButton_Click" >
                        <hc:SimpleStackPanel Orientation="Horizontal">
                            <Image Source="/static/AddRow.png" Height="30"/>
                            <TextBlock Text=" 新建条目" VerticalAlignment="Center"/>
                        </hc:SimpleStackPanel>
                    </Button>
                    <!--删除选中的条目
                        尚未实现的需求：
                            在没有可选条目的时候保持不可用
                            单机后弹窗提示二次确认-->
                    <Button Height="45" FontSize="14" Margin="5,0,5,0" Command="local:MainWindow.DeleteRowCommand">
                        <hc:SimpleStackPanel Orientation="Horizontal">
                            <Image Source="/static/DeleteRow.png" Height="30"/>
                            <TextBlock Text=" 删除条目" VerticalAlignment="Center"/>
                        </hc:SimpleStackPanel>
                    </Button>
                    <hc:Divider Orientation="Vertical" Margin="10,15,10,15" LineStrokeThickness="2" Height="Auto" LineStroke="#80000000"/>
                    <!--点按启动【AssistWindow】-->
                    <Button x:Name="StartServiceButton" Height="45" FontSize="14" Margin="5,0,5,0" Command="local:MainWindow.StartServiceCommand">
                        <hc:SimpleStackPanel Orientation="Horizontal">
                            <Image Source="/static/StartService.png" Height="30"/>
                            <TextBlock Text=" 启动服务" VerticalAlignment="Center"/>
                        </hc:SimpleStackPanel>
                    </Button>
                    <!--这个似乎可以删掉，设想是启动服务后这个窗口就隐藏了，【AssistWindow】负责停止-->
                    <!--
                    <Button Height="45" FontSize="14" Margin="5,0,5,0">
                        <hc:SimpleStackPanel Orientation="Horizontal">
                            <Image Source="/static/StopService.png" Height="30"/>
                            <TextBlock Text=" 停止服务" VerticalAlignment="Center"/>
                        </hc:SimpleStackPanel>
                    </Button>
                    -->
                    <hc:Divider Orientation="Vertical" Margin="10,15,10,15" LineStrokeThickness="2" Height="Auto" LineStroke="#80000000"/>
                    <!--暂时不知道该怎么做，涉及数据存储肯定要加密，带时候要改Student类-->
                    <Button Height="45" FontSize="14" Margin="5,0,5,0" >
                        <hc:SimpleStackPanel Orientation="Horizontal">
                            <Image Source="/static/LoadFrom.png" Height="30"/>
                            <TextBlock Text=" 从文件加载" VerticalAlignment="Center"/>
                        </hc:SimpleStackPanel>
                    </Button>
                    <Button Height="45" FontSize="14" Margin="5,0,5,0" >
                        <hc:SimpleStackPanel Orientation="Horizontal">
                            <Image Source="/static/SaveTo.png" Height="30"/>
                            <TextBlock Text=" 保存到" VerticalAlignment="Center"/>
                        </hc:SimpleStackPanel>
                    </Button>
                    <hc:Divider Orientation="Vertical" Margin="10,15,10,15" LineStrokeThickness="2" Height="Auto" LineStroke="#80000000"/>
                    <Button x:Name="SettingsButton" Height="45" FontSize="14" Margin="5,0,5,0" Click="SettingsButton_Click">
                        <hc:SimpleStackPanel Orientation="Horizontal">
                            <Image Source="/static/Settings.png" Height="30"/>
                            <TextBlock Text=" 设置" VerticalAlignment="Center"/>
                        </hc:SimpleStackPanel>
                    </Button>
                </hc:SimpleStackPanel>
            </Border>

            <hc:Divider Height="5"
                        Margin="5"
                        LineStrokeThickness="2"
                        LineStroke="#FF7D8F95"/>
            <!-- 优化后的DataGrid -->
            <DataGrid AutoGenerateColumns="False" Margin="5" x:Name="StudentDataGrid"
          ColumnHeaderHeight="30" RowHeight="30" 
          VerticalAlignment="Stretch" 
          VerticalContentAlignment="Stretch"
          Height="Auto"
          Background="#B3FFFFFF"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          CanUserAddRows="False"
          HeadersVisibility="Column"
          Opacity="1" HorizontalContentAlignment="Center" 
          CanUserReorderColumns="True" MouseDoubleClick="DataGrid_MouseDoubleClick" 
          IsReadOnly="True" ItemsSource="{Binding StudentCollection}" >
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource DataGridRowStyle}">
                        <Style.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="#6066CCFF"/>
                            </MultiTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource DataGridCellStyle}">
                        <Style.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="Transparent"/>
                            </MultiTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="姓名" Binding="{Binding StudentName}" Width="80"/>
                    <DataGridTextColumn Header="民族" Binding="{Binding StudentNation}" Width="60"/>
                    <DataGridTextColumn Header="注册时间" Binding="{Binding RegDate}" Width="80"/>
                    <DataGridTextColumn Header="身份证号" Binding="{Binding Pin}" Width="160"/>
                    <DataGridTextColumn Header="住址" Binding="{Binding Address}" Width="*"/>
                    <DataGridTextColumn Header="联系方式" Binding="{Binding Tel}" Width="120"/>
                    <DataGridCheckBoxColumn Header="是否注册志愿者" Binding="{Binding VolunteerState}" Width="110" CellStyle="{Binding Source={StaticResource DataGridComboBoxCenterColumnStyle}}"/>
                </DataGrid.Columns>
            </DataGrid>
        </hc:SimpleStackPanel>
    </Grid>
    <!--命令-->
    <hc:Window.CommandBindings>
        <CommandBinding Command="local:MainWindow.StartServiceCommand" CanExecute="StartService_CanExecute" Executed="StartService_Executed"/>
        <CommandBinding Command="local:MainWindow.DeleteRowCommand" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
    </hc:Window.CommandBindings>
</hc:Window>