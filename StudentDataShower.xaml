<UserControl x:Class="DocumentHelper.StudentDataShower"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocumentHelper"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="#FFFFFFFF" Height="40" Margin="20" VerticalAlignment="Center">
    <hc:SimpleStackPanel Orientation="Horizontal" Height="40" VerticalAlignment="Center">
        <hc:SimpleText Text="{Binding DataHint, RelativeSource={RelativeSource AncestorType=UserControl}}" TextAlignment="Left" MinWidth="100" VerticalAlignment="Center"/>
        <hc:SimpleText Text="{Binding DataResource, RelativeSource={RelativeSource AncestorType=UserControl}}" TextAlignment="Left" VerticalAlignment="Center" MinWidth="100"></hc:SimpleText>
    </hc:SimpleStackPanel>
</UserControl>
