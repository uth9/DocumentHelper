<hc:Window x:Class="DocumentHelper.CustomSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentHelper"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:properties="clr-namespace:DocumentHelper.Properties"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="CustomSettings" Height="450" Width="700" Icon="/Icon.png">
    <hc:Window.Resources>
        <ControlTemplate x:Key="SettingItemTemplate" >
            
        </ControlTemplate>
    </hc:Window.Resources>
    <Grid>
        <!-- 背景图片 -->
        <Image Source="/background4settings.png" Stretch="UniformToFill" Opacity="1" Margin="0"/>

        <!-- 磨砂玻璃层 -->
        <Border Background="#B3FFFFFF" CornerRadius="8" Margin="0">
            <Border.Effect>
                <BlurEffect Radius="15" KernelType="Gaussian"/>
            </Border.Effect>
        </Border>
        <hc:SimpleStackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <hc:SideMenu BorderThickness="1" Width="150" Margin="0">
                <hc:Interaction.Triggers>
                    <hc:EventTrigger EventName="SelectionChanged">
                        <hc:EventToCommand Command="{Binding SwitchItemCmd}" PassEventArgsToCommand="True" />
                    </hc:EventTrigger>
                </hc:Interaction.Triggers>
                <hc:SideMenuItem Header="Overview">
                    <hc:SideMenuItem.Icon>
                        <Image Source="/static/AddRow.png" Width="24" Height="24"/>
                    </hc:SideMenuItem.Icon>
                </hc:SideMenuItem>
            </hc:SideMenu>
            <hc:SimpleStackPanel>
                <local:SettingsBorder SettingText="默认设置项" IsChecked="True" Width="510" Height="40" Margin="20"/>
            </hc:SimpleStackPanel>
        </hc:SimpleStackPanel>
    </Grid>
</hc:Window>
